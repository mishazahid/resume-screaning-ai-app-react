[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/api/sample-jd"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"

[[services]]
name = "resume-screening-api"
