# FastAPI backend requirements (in addition to the core requirements.txt)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Core pipeline deps (already in requirements.txt — listed here for completeness)
sentence-transformers>=2.6.0
scikit-learn>=1.4.0
PyMuPDF>=1.23.0
numpy>=1.26.0
torch>=2.1.0
